apiVersion: v1
kind: Service
metadata:
  name: room-manager-agent
  namespace: meeting-platform
spec:
  selector:
    app: room-manager-agent
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

