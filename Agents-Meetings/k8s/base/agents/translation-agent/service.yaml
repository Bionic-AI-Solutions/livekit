apiVersion: v1
kind: Service
metadata:
  name: translation-agent
  namespace: meeting-platform
spec:
  selector:
    app: translation-agent
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

