apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: meeting-platform
data:
  # Backend-specific configuration only
  ENVIRONMENT: "production"
  LOG_LEVEL: "info"
  CORS_ORIGINS: "https://localhost:3000,https://meet.bionicaisolutions.com,https://bionicaisolutions.com"
  LIVEKIT_WS_URL: "wss://livekit.bionicaisolutions.com"
  DATABASE_HOST: "postgres-service"
  DATABASE_PORT: "5432"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  MAIL_API_URL: "https://mail.bionicaisolutions.com"

